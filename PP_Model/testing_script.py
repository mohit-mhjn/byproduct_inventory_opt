a = [2,23,5]
a_global = set()
for k in a:
    a1 = str(k)
    my_set = set()
    for a0 in a1:
        my_set.add(int(a0))
    print (my_set)
    a_global.add(list(my_set))

pandas.DataFrame(list(itertools.product(range(0,10),k1,k2)))
